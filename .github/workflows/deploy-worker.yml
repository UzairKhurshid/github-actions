name: Deploy Worker

on:
  workflow_dispatch:   # manual run from GitHub UI

  repository_dispatch:
    types: [run-worker-deploy]   # trigger from API/code

jobs:
  deploy-worker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Worker Image
        run: |
          echo "Building WORKER..."
          docker build -t my-worker -f Dockerfile.worker .

      - name: Deploy to ECS (Worker)
        run: |
          echo "Deploying WORKER to ECS..."
          # aws ecs update-service ...